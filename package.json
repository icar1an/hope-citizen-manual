{
  "name": "hope-citizen-manual",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:worker": "wrangler dev",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint src/ && stylelint 'src/**/*.css'",
    "lint:fix": "eslint src/ --fix && stylelint 'src/**/*.css' --fix",
    "format": "prettier --write 'src/**/*.{js,css}' index.html",
    "format:check": "prettier --check 'src/**/*.{js,css}' index.html",
    "quality": "npm run format && npm run lint:fix",
    "screenshot": "node tests/visual/screenshot.spec.js",
    "visual-diff": "node tests/visual/compare.js",
    "check-all": "npm run quality && npm run build && npm run screenshot && npm run visual-diff",
    "predeploy": "node -e \"if (require('fs').existsSync('FREEZE.md')) { console.error('\\n\\u274C  DEPLOY BLOCKED: FREEZE.md present at repo root. See FREEZE.md for unfreeze procedure.\\n'); process.exit(1); }\"",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "gsap": "^3.14.2",
    "suncalc": "^1.9.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.1.0",
    "eslint-config-prettier": "^10.1.8",
    "pixelmatch": "^7.1.0",
    "playwright": "^1.58.2",
    "pngjs": "^7.0.0",
    "prettier": "^3.8.1",
    "stylelint": "^17.5.0",
    "stylelint-config-standard": "^40.0.0",
    "stylelint-prettier": "^5.0.3",
    "vite": "^7.3.1",
    "wrangler": "^4.83.0",
    "ws": "^8.20.0"
  }
}
